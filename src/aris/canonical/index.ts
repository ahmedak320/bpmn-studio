/**
 * Barrel for the `CanonicalProcessV1` package entry (Lane L-SCHEMA, Wave 18).
 *
 * L-PROJECT (Wave 19) extends this file with the projection, findings, and
 * `buildVerificationPackage` re-exports — this lane owns only the contract
 * and the JSON Schema emitter.
 */

export * from './contract'
export * from './jsonSchema'
