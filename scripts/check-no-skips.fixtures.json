{
  "note": "Fixture strings for scripts/check-no-skips.test.mjs. These deliberately contain the literal substrings the release-test integrity checker looks for (fit(, describe.skip(, describe.runIf(, ...). They live in a .json file — outside check-no-skips.mjs's own scan globs (src/tests/**/*.{js,ts,...} and scripts/**/*.test.mjs) — so the fixtures themselves are never mistaken for real skipped/focused test code.",
  "focusedAliasGenuine": [
    "fit('does the thing', () => {})",
    "  xit(\"skips this\", () => {})",
    "fdescribe('focused suite', () => {})",
    "xdescribe(`focused suite`, () => {})",
    "  fit ('spaced paren', () => {})",
    "xtest('marks a focused/disabled test', () => {})"
  ],
  "focusedAliasFalsePositives": [
    "  fit(): number {\n    return this.canvas.zoom('fit-viewport')\n  }",
    "expect(canvas.fit()).toBeGreaterThan(0)",
    "this.canvas.fit()",
    "const result = zoomTarget.fit(level)",
    "function fit() { return 1 }"
  ],
  "skippedSuiteGenuine": [
    "describe.skip('x', () => {})",
    "describe.skipIf(cond)('x', () => {})",
    "it.todo('x')"
  ],
  "runIfGenuine": ["describe.runIf(available)('x', () => {})"],
  "fixtureGuardEvasions": {
    "variableHeldSkip": "import { existsSync } from 'node:fs'\nconst fixture = existsSync('../../../../reference/AnimalWF/ARISAMLExport.xml') ? readFixture() : null\nconst itIfFixture = fixture ? it : it.skip\nitIfFixture('uses the fixture', () => {})",
    "noOpAlias": "import { existsSync, readFileSync } from 'node:fs'\nconst ANIMAL_WF_PATH = '/abs/reference/AnimalWF/ARISAMLExport.xml'\nconst animalWfFixture = existsSync(ANIMAL_WF_PATH) ? readFileSync(ANIMAL_WF_PATH, 'utf8') : null\nconst itIfAnimalWf = animalWfFixture ? it : (_name, _fn) => undefined\nitIfAnimalWf('uses the fixture', () => {})",
    "describeEarlyReturn": "import fs from 'node:fs'\nconst FIXTURE_PATH = resolve(dirname, '../../../../reference/AnimalWF/ARISAMLExport.xml')\nconst FIXTURE_PRESENT = fs.existsSync(FIXTURE_PATH)\ndescribe('real data', () => {\n  if (!FIXTURE_PRESENT) {\n    it('is skipped', () => { expect(FIXTURE_PRESENT).toBe(false) })\n    return\n  }\n  it('uses the fixture', () => {})\n})",
    "tautologicalAbsenceAssertion": "import { existsSync } from 'node:fs'\nconst FIXTURE_PATH = resolve(here, '../../../../../reference/AnimalWF/ARISAMLExport.xml')\ndescribe('smoke test', () => {\n  it('builds digests', () => {\n    if (!existsSync(FIXTURE_PATH)) {\n      expect(existsSync(FIXTURE_PATH)).toBe(false)\n      return\n    }\n    doRealWork()\n  })\n})"
  },
  "fixtureGuardCorrectlyNamed": "import { existsSync } from 'node:fs'\nconst ANIMAL_WF_PATH = resolve('../../../../reference/AnimalWF/ARISAMLExport.xml')\nif (!existsSync(ANIMAL_WF_PATH)) {\n  throw new Error('AnimalWF fixture not found')\n}\ndescribe('AnimalWF', () => {\n  it('uses the fixture', () => {})\n})"
}
